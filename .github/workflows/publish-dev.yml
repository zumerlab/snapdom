name: Publish Dev PreRelease to GitHub Packages

on:
  push:
    branches:
      - dev

jobs:
  publish:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          registry-url: 'https://npm.pkg.github.com/'
          scope: '@laorg'

      - name: Install dependencies
        run: npm ci

      - name: Read base version
        id: version
        run: |
          BASE_VERSION=$(jq -r .version package.json)
          echo "Base version: $BASE_VERSION"
          echo "base_version=$BASE_VERSION" >> $GITHUB_OUTPUT

      - name: Compute next dev version
        id: devver
        run: |
          BASE=${{ steps.version.outputs.base_version }}
          # Get latest dev version published
          LAST=$(npm view @laorg/paquete versions --registry=https://npm.pkg.github.com/ 2>/dev/null | grep "${BASE}-dev" | sed "s/.*-dev.\([0-9]*\).*/\1/" | sort -n | tail -1)
          N=$((LAST + 1))
          DEV_VERSION="${BASE}-dev.${N}"
          echo "Computed version: $DEV_VERSION"
          echo "dev_version=$DEV_VERSION" >> $GITHUB_OUTPUT

      - name: Set dev version temporarily
        run: |
          npm version ${{ steps.devver.outputs.dev_version }} --no-git-tag-version

      - name: Publish to GitHub Packages
        run: npm publish --registry=https://npm.pkg.github.com/
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
